---
phase: 07-playground-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.playground.ts
  - playground/index.html
  - package.json
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03

must_haves:
  truths:
    - "Running `npm run playground` opens a browser page with the chat widget rendered"
    - "Changes to widget source files hot-reload in the playground without manual refresh"
    - "Running `npm run build` produces CDN and npm bundles that contain zero playground code"
  artifacts:
    - path: "vite.config.playground.ts"
      provides: "Vite dev server config for playground"
      contains: "server"
    - path: "playground/index.html"
      provides: "Playground HTML page with widget tag"
      contains: "w1-chat-widget"
    - path: "package.json"
      provides: "playground npm script"
      contains: "playground"
  key_links:
    - from: "playground/index.html"
      to: "src/index.ts"
      via: "script module import"
      pattern: "src/index"
    - from: "package.json"
      to: "vite.config.playground.ts"
      via: "npm script reference"
      pattern: "vite.*vite.config.playground"
---

<objective>
Create the dev playground infrastructure: a Vite-served HTML page that renders the chat widget from local source, with hot reload, fully excluded from production bundles.

Purpose: Give developers a local testing surface for the widget without affecting distribution builds.
Output: Three files — Vite playground config, playground HTML page, updated package.json script.
</objective>

<execution_context>
@/home/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@vite.config.ts
@vite.config.iife.ts
@package.json
@src/index.ts
</context>

<interfaces>
<!-- Existing Vite config pattern (from vite.config.ts): -->
```typescript
import { defineConfig } from 'vite';
// Library mode with entry: 'src/index.ts'
```

<!-- Widget web component tag (from src/work1-chat-widget.ts): -->
```html
<w1-chat-widget></w1-chat-widget>
```

<!-- Public API barrel (from src/index.ts): -->
```typescript
export { Work1ChatWidget } from './work1-chat-widget.js';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite playground config and HTML page</name>
  <files>vite.config.playground.ts, playground/index.html</files>
  <action>
Create `vite.config.playground.ts` at project root using `defineConfig` from Vite:
- `root: 'playground'` — serves from playground directory
- `server.port: 5180` — fixed port per user decision, avoids conflict with default 5173
- `server.open: true` — auto-open browser on launch per user decision
- No `build` section needed — this config is dev-only
- No plugins needed (no dts, no library mode)

Create `playground/index.html`:
- Standard HTML5 boilerplate
- Title: "Work1 Chat Widget Playground"
- Clean white background, no surrounding chrome per user decision
- A `<script type="module">` that imports from `../src/index.ts` (Vite resolves the relative path from playground root)
- A `<w1-chat-widget></w1-chat-widget>` element in the body — mirrors customer usage per user decision
- Minimal inline styles: white background, full viewport height, no margin/padding
- No dark mode, no placeholder for control panel per user decision

Update `package.json` scripts — add:
```json
"playground": "vite --config vite.config.playground.ts"
```
Add it after the existing `dev` script entry.

Do NOT modify `vite.config.ts` or `vite.config.iife.ts`.
  </action>
  <verify>
    <automated>npm run playground -- --host 127.0.0.1 2>&1 | head -5</automated>
  </verify>
  <done>
  - `npm run playground` starts Vite dev server on port 5180
  - Playground page contains `<w1-chat-widget>` tag
  - Widget source imported from local `src/index.ts`
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify bundle exclusion</name>
  <files></files>
  <action>
Run `npm run build` and verify playground code is excluded:
1. Run full build: `npm run build`
2. Check that `playground/` content does not appear in any dist file — search dist/ for "playground" and "5180"
3. Verify `package.json` `files` field still only includes `["dist", "README.md"]` — this already excludes playground/ from npm pack
4. Run `npm pack --dry-run` and confirm no playground files appear in the tarball listing

No code changes expected. This is a verification-only task. If build fails or playground code leaks into bundles, fix the root cause (likely a Vite config issue in Task 1).
  </action>
  <verify>
    <automated>npm run build && npm pack --dry-run 2>&1 | grep -c "playground" | xargs test 0 -eq && echo "PASS: no playground in bundle"</automated>
  </verify>
  <done>
  - `npm run build` succeeds without errors
  - No playground files appear in dist/ output
  - `npm pack --dry-run` shows zero playground files
  </done>
</task>

</tasks>

<verification>
1. `npm run playground` starts dev server on port 5180 and auto-opens browser
2. Playground page renders the `<w1-chat-widget>` web component from local source
3. Editing a widget source file triggers Vite hot reload (full page reload expected)
4. `npm run build` succeeds and dist/ contains zero playground references
5. `npm pack --dry-run` lists no playground files
</verification>

<success_criteria>
- Developers can run `npm run playground` to get a live widget testing page
- Widget loads from local source, not pre-built bundles
- Production builds remain clean — zero playground code in dist or npm package
</success_criteria>

<output>
After completion, create `.planning/phases/07-playground-infrastructure/07-01-SUMMARY.md`
</output>
