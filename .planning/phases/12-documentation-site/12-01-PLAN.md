---
phase: 12-documentation-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/.vitepress/config.ts
  - docs/.vitepress/theme/index.ts
  - docs/.vitepress/theme/custom.css
  - docs/index.md
  - docs/integration.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "VitePress dev server starts and renders the landing page without errors"
    - "docs:build completes successfully producing static output"
    - "Landing page shows hero with tagline, install command, and links to guides"
    - "Integration guide explains script tag installation with CDN URL and code example"
    - "Integration guide explains npm installation with import and code example"
  artifacts:
    - path: "docs/.vitepress/config.ts"
      provides: "VitePress configuration with base path, custom element support, sidebar, nav"
      contains: "isCustomElement"
    - path: "docs/index.md"
      provides: "Hero landing page with layout: home frontmatter"
      contains: "layout: home"
    - path: "docs/integration.md"
      provides: "Integration guide with script tag and npm methods"
      contains: "npm"
  key_links:
    - from: "package.json"
      to: "docs/.vitepress/config.ts"
      via: "docs:dev, docs:build, docs:preview scripts"
      pattern: "vitepress (dev|build|preview) docs"
    - from: "docs/index.md"
      to: "docs/integration.md"
      via: "hero action link"
      pattern: "/integration"
---

<objective>
Set up VitePress documentation infrastructure and create the landing page and integration guide.

Purpose: Establish the docs site foundation (DOCS-01) so content pages can be added in parallel, and deliver the first content page (DOCS-02) covering both script tag and npm installation methods.
Output: Working VitePress site with hero landing page and integration guide page.
</objective>

<execution_context>
@/home/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-documentation-site/12-RESEARCH.md
@.planning/phases/12-documentation-site/12-CONTEXT.md

@package.json
@src/work1-chat-widget.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up VitePress infrastructure and landing page</name>
  <files>package.json, docs/.vitepress/config.ts, docs/.vitepress/theme/index.ts, docs/.vitepress/theme/custom.css, docs/index.md</files>
  <action>
1. Install VitePress: `npm add -D vitepress@latest`

2. Add scripts to package.json:
   - `"docs:dev": "vitepress dev docs"`
   - `"docs:build": "vitepress build docs"`
   - `"docs:preview": "vitepress preview docs"`

3. Create `docs/.vitepress/config.ts`:
   - `title: 'Work1 Chat Widget'`
   - `description: 'Embeddable AI chat widget documentation'`
   - `base: '/widget/'` (for GitHub Pages at work1ai.github.io/widget/)
   - `vue.template.compilerOptions.isCustomElement` targeting ONLY `work1-chat-widget` tag (NOT a broad `tag.includes('-')` check)
   - Do NOT re-register `@vitejs/plugin-vue` in vite.plugins -- use the top-level `vue` key per VitePress docs
   - `vite.resolve.alias` mapping `@work1ai/chat-widget` to `../src/index.ts` (use `fileURLToPath` + `import.meta.url` for path resolution)
   - Sidebar with 4 items: Integration Guide (/integration), Theming (/theming), API Reference (/api), Events & Connection (/events)
   - Nav: minimal -- just logo/title and GitHub link (https://github.com/work1ai/widget)
   - Social links: GitHub icon linking to repo
   - Enable local search: `themeConfig.search: { provider: 'local' }`

4. Create `docs/.vitepress/theme/index.ts`:
   - Extend DefaultTheme
   - Import custom.css
   - No widget import here (handled per-page via dynamic import in onMounted)

5. Create `docs/.vitepress/theme/custom.css`:
   - Minimal overrides only -- VitePress default theme handles most styling
   - Set brand color CSS variable to match widget accent (#0066FF): `--vp-c-brand-1: #0066FF;` etc.

6. Create `docs/index.md` with hero landing page:
   - Frontmatter: `layout: home`
   - Hero: name "Work1 Chat Widget", text "Embeddable AI Chat", tagline "Add a chat widget to any website with one line of HTML"
   - Actions: "Get Started" (brand theme, links to /integration), "API Reference" (alt theme, links to /api)
   - Features (3 items): "Drop-in Integration" (Script tag or npm), "Fully Themeable" (CSS custom properties and ::part()), "Real-time Streaming" (WebSocket-powered with markdown rendering)
   - Getting started content on this page (not a separate page) per user decision: add a brief section below the hero showing the quick-start snippet:
     ```html
     <script src="https://unpkg.com/@work1ai/chat-widget"></script>
     <work1-chat-widget server-url="wss://your-server.com/ws"></work1-chat-widget>
     ```

7. Add `docs/.vitepress/cache` and `docs/.vitepress/dist` to `.gitignore` if not already ignored.

8. Verify: run `npm run docs:dev` briefly to confirm site starts, then `npm run docs:build` to confirm build succeeds.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/work1/widget && npm run docs:build</automated>
  </verify>
  <done>VitePress site builds successfully. Landing page renders with hero, features, and quick-start snippet. docs:dev server starts without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write integration guide page</name>
  <files>docs/integration.md</files>
  <action>
Create `docs/integration.md` covering both installation methods (DOCS-02). Structure:

1. **Page title**: "Integration Guide"

2. **Script Tag (CDN)** section:
   - Explain this is the simplest method -- one line of HTML
   - Code example using unpkg CDN:
     ```html
     <script src="https://unpkg.com/@work1ai/chat-widget"></script>
     <work1-chat-widget server-url="wss://your-server.com/ws"></work1-chat-widget>
     ```
   - Note: also available via jsDelivr: `https://cdn.jsdelivr.net/npm/@work1ai/chat-widget`
   - Explain the IIFE bundle (116 KB) is self-contained with all dependencies

3. **npm Package** section:
   - Install command: `npm install @work1ai/chat-widget`
   - Import: `import '@work1ai/chat-widget'` (side-effect import registers the custom element)
   - Usage in HTML after import:
     ```html
     <work1-chat-widget server-url="wss://your-server.com/ws"></work1-chat-widget>
     ```
   - Note the ESM bundle externalizes dependencies (lit, marked, dompurify)

4. **Configuration** section:
   - Show common attributes inline: chat-title, chat-subtitle, greeting, position, primary-color
   - Example with multiple attributes:
     ```html
     <work1-chat-widget
       server-url="wss://your-server.com/ws"
       chat-title="Support"
       chat-subtitle="We usually reply in minutes"
       greeting="Hello! How can I help you today?"
       position="bottom-left"
       primary-color="#7c3aed"
     ></work1-chat-widget>
     ```
   - Link to API Reference for full attribute list
   - Link to Theming guide for CSS customization

5. **Content guidelines per user decision:**
   - Minimal, focused snippets -- one concept per block
   - Copy-paste ready (VitePress has built-in copy button)
   - Vanilla HTML/JS only -- NO React/Vue/Angular framework examples
   - Scan-friendly: developers want to copy-paste, not read paragraphs
  </action>
  <verify>
    <automated>cd /home/apollo/Development/work1/widget && npm run docs:build</automated>
  </verify>
  <done>Integration guide page builds and appears in sidebar. Contains both script tag and npm installation methods with working code examples. No framework-specific examples.</done>
</task>

</tasks>

<verification>
- `npm run docs:build` completes without errors
- Built site contains index.html (landing) and integration.html
- Sidebar shows all 4 navigation items
- Landing page has hero with features and quick-start code
- Integration page has CDN and npm sections with code blocks
</verification>

<success_criteria>
- VitePress site builds successfully with `npm run docs:build`
- Landing page renders hero with tagline, install command, and guide links
- Integration guide covers both script tag (CDN) and npm installation with copy-paste code examples
- Site configured with base `/widget/` for GitHub Pages
- Custom element `work1-chat-widget` does not trigger Vue warnings
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation-site/12-01-SUMMARY.md`
</output>
