---
phase: 12-documentation-site
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - docs/api.md
  - docs/theming.md
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "API reference page lists all 11 HTML attributes with type, default, and description"
    - "API reference page lists all 12 CSS custom properties with defaults and descriptions"
    - "API reference page lists all 5 CSS ::part() selectors"
    - "Theming guide shows how to customize colors using CSS custom properties"
    - "Theming guide shows how to use ::part() selectors with examples"
    - "Theming guide includes 3 copy-paste theme recipes: dark mode, brand color, compact"
    - "Theming guide documents position and size customization"
  artifacts:
    - path: "docs/api.md"
      provides: "API reference with attribute and CSS property tables"
      contains: "--w1-accent-color"
    - path: "docs/theming.md"
      provides: "Theming guide with recipes and visual examples"
      contains: "::part"
  key_links:
    - from: "docs/integration.md"
      to: "docs/api.md"
      via: "cross-reference link"
      pattern: "/api"
    - from: "docs/integration.md"
      to: "docs/theming.md"
      via: "cross-reference link"
      pattern: "/theming"
---

<objective>
Write the API reference and theming guide documentation pages.

Purpose: Deliver complete attribute/property reference (DOCS-03) and theming customization guide with recipes (DOCS-04) so widget consumers can configure and style the widget.
Output: Two documentation pages with tables, code examples, and theme recipes.
</objective>

<execution_context>
@/home/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-documentation-site/12-RESEARCH.md
@.planning/phases/12-documentation-site/12-CONTEXT.md
@.planning/phases/12-documentation-site/12-01-SUMMARY.md

@src/work1-chat-widget.ts
@src/styles/widget.styles.ts
@src/styles/bubble.styles.ts
@src/styles/header.styles.ts
@src/styles/messages.styles.ts
@src/styles/input.styles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write API reference page</name>
  <files>docs/api.md</files>
  <action>
Create `docs/api.md` as a comprehensive API reference (DOCS-03). Structure:

1. **Page title**: "API Reference"

2. **HTML Attributes** section:
   - Compact table format per user decision: Attribute | Type | Default | Description
   - List ALL 11 attributes from source (verify against src/work1-chat-widget.ts @property decorators):
     - server-url (String, '', WebSocket server URL)
     - debug (Boolean, false, Enable debug logging)
     - chat-title (String, 'Chat', Header title text)
     - chat-subtitle (String, '', Subtitle below header title)
     - placeholder (String, 'Type a message...', Input placeholder)
     - greeting (String, '', Greeting message on connect)
     - position (String, 'bottom-right', Panel position: bottom-right or bottom-left)
     - width (String, '', Panel width override)
     - height (String, '', Panel height override)
     - primary-color (String, '', Accent color override)
     - bubble-icon (String, '', Lucide icon name for bubble)
   - Brief usage example after the table showing key attributes

3. **CSS Custom Properties** section:
   - Table format: Property | Default | Description
   - List ALL 12 properties from source styles:
     - --w1-accent-color (#0066FF)
     - --w1-panel-width (380px)
     - --w1-panel-height (560px)
     - --w1-panel-bg (#ffffff)
     - --w1-border-color (#e5e5e5)
     - --w1-input-bg (#ffffff)
     - --w1-input-field-bg (#f8f8f8)
     - --w1-user-bg (var(--w1-accent-color, #0066FF))
     - --w1-agent-bg (#f0f0f0)
     - --w1-agent-color (#1a1a1a)
     - --w1-error-color (#dc3545)
     - --w1-error-bg (#fef2f2)
   - Brief CSS usage example:
     ```css
     work1-chat-widget {
       --w1-accent-color: #7c3aed;
       --w1-panel-bg: #fafafa;
     }
     ```

4. **CSS ::part() Selectors** section:
   - Table: Part | Element | Description
   - List all 5 parts: bubble, panel, header, message-list, input
   - Usage example:
     ```css
     work1-chat-widget::part(header) {
       border-bottom: 2px solid #0066FF;
     }
     ```

5. **Content guidelines:**
   - Tables are the primary format (per user decision: compact table format)
   - Verify every attribute and property against source code -- no drift
   - One focused code example per section
   - Cross-link to Theming guide for deeper customization guidance
  </action>
  <verify>
    <automated>cd /home/apollo/Development/work1/widget && npm run docs:build</automated>
  </verify>
  <done>API reference page lists all 11 HTML attributes, all 12 CSS custom properties, and all 5 ::part() selectors in table format with code examples. All values match source code.</done>
</task>

<task type="auto">
  <name>Task 2: Write theming guide with recipes and live demo</name>
  <files>docs/theming.md</files>
  <action>
Create `docs/theming.md` as a theming and customization guide (DOCS-04). Structure:

1. **Page title**: "Theming"

2. **Overview** -- brief intro explaining the three customization layers:
   - HTML attributes (primary-color, position, width, height) -- quickest
   - CSS custom properties (--w1-* variables) -- fine-grained
   - CSS ::part() selectors -- full CSS access to internal elements

3. **Quick Customization** section:
   - Using primary-color attribute for simple accent color change
   - Using position attribute (bottom-right / bottom-left)
   - Using width and height attributes
   - Code example:
     ```html
     <work1-chat-widget
       primary-color="#7c3aed"
       position="bottom-left"
       width="400px"
       height="600px"
     ></work1-chat-widget>
     ```

4. **CSS Custom Properties** section:
   - Reference table: Property | Default | Description (same data as API reference but in theming context)
   - Themed code examples alongside the table (per user decision)
   - Show how to target the element:
     ```css
     work1-chat-widget {
       --w1-accent-color: #7c3aed;
       --w1-panel-bg: #fafafa;
     }
     ```

5. **::part() Selectors** section:
   - Explain ::part() allows styling shadow DOM internals
   - Usage examples for each part:
     ```css
     work1-chat-widget::part(bubble) { /* ... */ }
     work1-chat-widget::part(panel) { /* ... */ }
     work1-chat-widget::part(header) { /* ... */ }
     ```
   - Link to API Reference for full part list

6. **Theme Recipes** section (per user decision: 2-3 pre-built, copy-paste ready):

   **Dark Mode:**
   ```css
   work1-chat-widget {
     --w1-panel-bg: #1e1e2e;
     --w1-agent-bg: #2a2a3e;
     --w1-agent-color: #e0e0e0;
     --w1-input-bg: #1e1e2e;
     --w1-input-field-bg: #2a2a3e;
     --w1-border-color: #3a3a4e;
     --w1-accent-color: #818cf8;
   }
   ```

   **Brand Color (Purple):**
   ```css
   work1-chat-widget {
     --w1-accent-color: #7c3aed;
   }
   ```

   **Compact/Minimal:**
   ```css
   work1-chat-widget {
     --w1-panel-width: 320px;
     --w1-panel-height: 420px;
   }
   ```

7. **Live Widget Demo** -- embed 1 live widget demo on this page (Claude's discretion: theming page is the best location since users can see the widget they're learning to customize):
   - Use `<script setup>` with `onMounted` and dynamic `import('@work1ai/chat-widget')` to avoid SSR issues
   - Wrap in a `position: relative` container with explicit height and border to constrain the fixed-position widget
   - Set attributes: chat-title="Demo Widget", chat-subtitle="Try it out", primary-color="#6366f1"
   - No server-url needed -- widget renders bubble and panel UI without WebSocket
   - If the position:relative container does not properly constrain the widget, fall back to a note explaining this is a visual demo

8. **Content guidelines:**
   - Recipes must be copy-paste ready CSS blocks (per user decision)
   - Themed code examples alongside reference tables (per user decision)
   - Position and size documented within this guide (per user decision)
  </action>
  <verify>
    <automated>cd /home/apollo/Development/work1/widget && npm run docs:build</automated>
  </verify>
  <done>Theming guide includes CSS custom properties table, ::part() examples, 3 theme recipes (dark, brand color, compact), position/size docs, and a live widget demo. All recipes are copy-paste ready.</done>
</task>

</tasks>

<verification>
- `npm run docs:build` completes without errors
- API reference contains 11 attributes, 12 CSS properties, 5 ::part() selectors
- Theming guide contains 3 theme recipes
- Live widget demo renders in VitePress (no SSR errors)
- All values match source code (no documentation drift)
</verification>

<success_criteria>
- API reference lists every HTML attribute and CSS custom property with descriptions and defaults
- Theming guide shows how to customize colors, position, and size
- 3 pre-built theme recipes are copy-paste ready
- Live widget demo renders on the theming page
- All documentation matches source code values exactly
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation-site/12-02-SUMMARY.md`
</output>
