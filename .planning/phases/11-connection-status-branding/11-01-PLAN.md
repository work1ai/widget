---
phase: 11-connection-status-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat-header.ts
  - src/styles/panel.styles.ts
  - src/work1-chat-widget.ts
  - src/work1-chat-widget.test.ts
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
  - BRAND-01
  - BRAND-02

must_haves:
  truths:
    - "A green dot appears next to the title when WebSocket is connected"
    - "A yellow dot appears next to the title when WebSocket is connecting or reconnecting"
    - "A red dot appears next to the title when WebSocket is disconnected"
    - "The badge reads 'Powered by work1.ai' instead of 'Powered by AI'"
    - "Clicking the badge opens https://work1.ai in a new tab"
  artifacts:
    - path: "src/components/chat-header.ts"
      provides: "renderHeader with connectionState parameter and status dot"
      contains: "status-dot"
    - path: "src/styles/panel.styles.ts"
      provides: "CSS for status dot colors and badge link styling"
      contains: "status-dot--connected"
    - path: "src/work1-chat-widget.ts"
      provides: "Passes store.connectionState to renderHeader"
      contains: "this.store.connectionState"
    - path: "src/work1-chat-widget.test.ts"
      provides: "Tests for all 5 requirements"
      contains: "status-dot"
  key_links:
    - from: "src/work1-chat-widget.ts"
      to: "src/components/chat-header.ts"
      via: "renderHeader call with connectionState arg"
      pattern: "renderHeader\\(this\\.chatTitle.*this\\.store\\.connectionState"
    - from: "src/components/chat-header.ts"
      to: "src/styles/panel.styles.ts"
      via: "classMap mapping connectionState to CSS classes"
      pattern: "status-dot--connected.*connectionState"
---

<objective>
Add connection status indicator and updated branding to the chat widget header.

Purpose: Users see real-time connection state feedback (green/yellow/red dot) and the badge links to work1.ai.
Output: Updated header with status dot and branded link badge, plus unit tests.
</objective>

<execution_context>
@/home/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-connection-status-branding/11-RESEARCH.md

@src/components/chat-header.ts
@src/styles/panel.styles.ts
@src/work1-chat-widget.ts
@src/work1-chat-widget.test.ts
@src/chat-store.types.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/chat-store.types.ts:
```typescript
export type ConnectionState =
  | 'disconnected'
  | 'connecting'
  | 'connected'
  | 'reconnecting';
```

From src/components/chat-header.ts (current signature):
```typescript
export function renderHeader(
  title: string,
  subtitle: string,
  onClose: () => void,
): TemplateResult
```

From src/work1-chat-widget.ts (current call site, line ~150):
```typescript
renderHeader(this.chatTitle, this.chatSubtitle, () => this.handleClose())
```

From src/styles/panel.styles.ts (existing badge style):
```css
.header-badge {
  font-size: 11px;
  opacity: 0.75;
  margin-right: 12px;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add status dot and branding link to header</name>
  <files>src/components/chat-header.ts, src/styles/panel.styles.ts, src/work1-chat-widget.ts, src/work1-chat-widget.test.ts</files>
  <behavior>
    - CONN-01: When connectionState is 'connected', a `.status-dot--connected` element exists inside `.header-title` with green background
    - CONN-02: When connectionState is 'connecting', a `.status-dot--connecting` element exists with yellow background
    - CONN-02 (reconnecting): When connectionState is 'reconnecting', a `.status-dot--connecting` element exists (same yellow)
    - CONN-03: When connectionState is 'disconnected', a `.status-dot--disconnected` element exists with red background
    - BRAND-01: The `.header-badge` text content is "Powered by work1.ai" (not "Powered by AI")
    - BRAND-02: The `.header-badge` is an `<a>` tag with href="https://work1.ai", target="_blank", rel="noopener noreferrer"
  </behavior>
  <action>
    **Tests first** — Add a new `describe('connection status dot')` block and a `describe('branding badge')` block to `src/work1-chat-widget.test.ts`. Write tests for all 6 behaviors above. The tests should set `store.connectionState` to each value and assert the shadow DOM contains the correct `.status-dot--{state}` class. For branding, assert badge text and link attributes.

    **Then implement:**

    1. **`src/components/chat-header.ts`** — Change `renderHeader` signature to accept `connectionState: ConnectionState` as the third parameter (shifting `onClose` to fourth). Import `classMap` from `lit/directives/class-map.js` and `ConnectionState` from `../chat-store.types.js`. Add a `<span>` with classMap-driven classes (`status-dot`, `status-dot--connected`, `status-dot--connecting`, `status-dot--disconnected`) inside `.header-title`, before the title text. Map `'reconnecting'` to the `connecting` class (yellow). Add `aria-label` with human-readable status. Change badge from `<span class="header-badge">Powered by AI</span>` to `<a class="header-badge" href="https://work1.ai" target="_blank" rel="noopener noreferrer">Powered by work1.ai</a>`.

    2. **`src/styles/panel.styles.ts`** — Add CSS rules after `.header-title`:
       - `.status-dot`: `display: inline-block; width: 8px; height: 8px; border-radius: 50%; margin-right: 6px; vertical-align: middle; flex-shrink: 0;`
       - `.status-dot--connected`: `background-color: #22c55e;` (green)
       - `.status-dot--connecting`: `background-color: #eab308;` (yellow)
       - `.status-dot--disconnected`: `background-color: #ef4444;` (red)
       - `a.header-badge`: `color: inherit; text-decoration: none;`
       - `a.header-badge:hover`: `text-decoration: underline;`

    3. **`src/work1-chat-widget.ts`** — Update the `renderHeader` call (around line 150) to pass `this.store.connectionState` as the third argument: `renderHeader(this.chatTitle, this.chatSubtitle, this.store.connectionState, () => this.handleClose())`.
  </action>
  <verify>
    <automated>cd /home/apollo/Development/work1/widget && npx vitest run src/work1-chat-widget.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 5 requirements verified: green/yellow/red dot renders for connected/connecting/disconnected states, reconnecting maps to yellow, badge reads "Powered by work1.ai" as a link to https://work1.ai with target="_blank". All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npm test` — full test suite passes (no regressions)
- `npm run build` — builds without TypeScript errors
- Status dot visually renders in playground for each connection state
</verification>

<success_criteria>
1. Green dot visible in header when connected
2. Yellow dot visible when connecting or reconnecting
3. Red dot visible when disconnected
4. Badge reads "Powered by work1.ai" and is a clickable link
5. All existing tests pass, new tests cover all 5 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/11-connection-status-branding/11-01-SUMMARY.md`
</output>
